services:
  mcp-server:
    build: .
    container_name: mcp-tools
    network_mode: host
    volumes:
      - ~/.aws:/root/.aws:ro
      - ~/.config/gh:/root/.config/gh:ro
      - ~/.ssh:/root/.ssh
      - ~/.kube:/root/.kube:ro
      - ${PWD}:/workspace
    working_dir: /workspace
    env_file:
      - .env
    environment:
      - AWS_PROFILE=${AWS_PROFILE:-mittentech-production}
      - GH_TOKEN=${GH_TOKEN}
    command: tail -f /dev/null
    stdin_open: true
    tty: true
